import React from 'react';
import PropTypes from 'prop-types';

const MainList = props => {
    return (
        <div>
            我是mainlist
        </div>
    );
};

MainList.propTypes = {

};

export default MainList;